# .env  --  NAO vai para o Git (esta' no .gitignore)

# AGORA: SQLite. Nao instala nada, nao tem senha, nao tem servico para
# subir -- o banco e' um arquivo que nasce sozinho aqui em backend/.
DATABASE_URL=sqlite:///./biblioteca.db

# NO PASSO 20: PostgreSQL. Troque a linha de cima por esta e nenhum
# arquivo dentro de app/ muda. E' a prova de que separar as camadas
# serviu para alguma coisa.
# DATABASE_URL=postgresql+psycopg://biblioteca:senha_secreta@localhost:5432/biblioteca
